#include "main.h"

/**
 * _strlen - count of a how many length in words
 * @s: the string to be measured
 *
 *
 * Return: the length of the string;
 *
 *
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0' ; i++)
	{
	}
	return (i);
}
